# ContentGenius 🚀

AI-платформа для генерации контента. Создавайте качественные тексты для блогов, социальных сетей, email-рассылок и рекламы за считанные секунды.

![ContentGenius](https://via.placeholder.com/800x400/8b5cf6/ffffff?text=ContentGenius)

## ✨ Возможности

- 📝 **Статьи для блога** - Создавайте увлекательные статьи
- 📱 **Посты для соцсетей** - Вирусный контент для Instagram, Twitter, LinkedIn
- 📧 **Email-рассылки** - Эффективные письма с высокой конверсией
- 🛍️ **Описания товаров** - Продающие описания для e-commerce
- 🎯 **Рекламные тексты** - Цепляющие объявления
- 🔍 **SEO-контент** - Оптимизированные для поиска тексты

## 💰 Бизнес-модель

| План | Цена | Генерации/день |
|------|------|----------------|
| Free | $0 | 5 |
| Pro | $19/мес | 100 |
| Business | $49/мес | Безлимит |

**Потенциальный доход при $10k/месяц:**
- ~350 Pro подписчиков, или
- ~140 Business подписчиков, или
- ~200 Pro + ~40 Business подписчиков

## 🛠️ Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Prisma** - ORM для базы данных
- **NextAuth.js** - Аутентификация
- **Stripe** - Платежи и подписки
- **SQLite** - База данных (замените на PostgreSQL в продакшене)

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
git clone <your-repo-url>
cd contentgenius
npm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

```env
# Database
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="your-super-secret-key"
NEXTAUTH_URL="http://localhost:3000"

# Stripe
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PRO_PRICE_ID="price_..."
STRIPE_BUSINESS_PRICE_ID="price_..."

# OpenAI (для реальной генерации контента)
OPENAI_API_KEY="sk-..."
```

### 3. Настройка базы данных

```bash
npx prisma db push
npx prisma generate
```

### 4. Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📦 Структура проекта

```
contentgenius/
├── prisma/
│   └── schema.prisma      # Схема базы данных
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   ├── auth/      # NextAuth endpoints
│   │   │   ├── generate/  # Генерация контента
│   │   │   ├── history/   # История генераций
│   │   │   ├── stripe/    # Stripe webhooks & checkout
│   │   │   └── user/      # Данные пользователя
│   │   ├── auth/          # Страницы авторизации
│   │   ├── dashboard/     # Главный дашборд
│   │   ├── history/       # История генераций
│   │   ├── pricing/       # Страница тарифов
│   │   └── page.tsx       # Landing page
│   ├── components/
│   │   ├── ui/            # UI компоненты
│   │   ├── navbar.tsx     # Навигация
│   │   └── footer.tsx     # Футер
│   └── lib/
│       ├── auth.ts        # Конфигурация NextAuth
│       ├── prisma.ts      # Prisma клиент
│       ├── stripe.ts      # Stripe конфигурация
│       └── utils.ts       # Утилиты
└── ...
```

## 🔧 Настройка Stripe

### 1. Создайте аккаунт Stripe

Зарегистрируйтесь на [stripe.com](https://stripe.com)

### 2. Создайте продукты и цены

В Stripe Dashboard:
1. Перейдите в Products
2. Создайте продукт "Pro" с ценой $19/месяц (recurring)
3. Создайте продукт "Business" с ценой $49/месяц (recurring)
4. Скопируйте Price IDs в `.env`

### 3. Настройте Webhook

1. Перейдите в Developers → Webhooks
2. Добавьте endpoint: `https://your-domain.com/api/stripe/webhook`
3. Выберите события:
   - `checkout.session.completed`
   - `invoice.payment_succeeded`
   - `customer.subscription.deleted`
4. Скопируйте Webhook Secret в `.env`

## 🤖 Интеграция с OpenAI

Для реальной генерации контента:

1. Получите API ключ на [platform.openai.com](https://platform.openai.com)
2. Добавьте `OPENAI_API_KEY` в `.env`
3. Раскомментируйте код OpenAI в `src/app/api/generate/route.ts`

```typescript
import OpenAI from "openai";

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

// В функции generateContent:
const response = await openai.chat.completions.create({
  model: "gpt-4",
  messages: [
    { role: "system", content: template.system },
    { role: "user", content: template.prefix + prompt },
  ],
});
return response.choices[0].message.content;
```

## 🚀 Деплой

### Vercel (рекомендуется)

```bash
npm install -g vercel
vercel
```

### Важно для продакшена:

1. Замените SQLite на PostgreSQL
2. Обновите `DATABASE_URL` на PostgreSQL connection string
3. Настройте Stripe webhook для продакшен домена
4. Используйте Stripe Live ключи вместо Test

## 📈 Масштабирование до $10k/месяц

### Маркетинг:
- SEO-оптимизация лендинга
- Контент-маркетинг (используйте свой же продукт!)
- Реклама в Google/Facebook
- Партнёрская программа

### Улучшения продукта:
- Интеграция с социальными сетями
- API для разработчиков
- Командный доступ
- Кастомные шаблоны
- Мультиязычность

### Удержание клиентов:
- Email-онбординг
- Туториалы и документация
- Быстрая поддержка
- Регулярные обновления

## 📄 Лицензия

MIT

## 🤝 Поддержка

По вопросам: support@contentgenius.ai

---

Сделано с ❤️ и AI
